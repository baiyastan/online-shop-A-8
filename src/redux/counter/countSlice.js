import { createSlice } from "@reduxjs/toolkit";

const countSlece = createSlice({
  name: "count",
  initialState: {
    count: 0,
  },
  reducers: {},
});

export const {} = countSlece.actions;
export default countSlece.reducer;
